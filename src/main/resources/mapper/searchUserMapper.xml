<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.autumn.weather.mapper.SearchUserMapper">
    <select id="searchUserMapper" resultType="com.autumn.weather.entity.User">
        select * from user
    </select>

    <delete id="truncateUser">
        truncate table user;
        truncate table test;
    </delete>

</mapper>

